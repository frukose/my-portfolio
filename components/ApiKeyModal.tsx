import React from 'react';

/**
 * ApiKeyModal removed per guidelines: 
 * "Do not generate any UI elements (input fields, forms, prompts, configuration sections) for entering or managing the API key."
 */
export const ApiKeyModal: React.FC = () => {
  return null;
};